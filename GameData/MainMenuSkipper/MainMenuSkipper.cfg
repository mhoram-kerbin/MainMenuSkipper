MAIN_MENU_SKIPPER_CONFIG
{
    // flag if the mod is active (true false)
    active = true

    // name of the save game to load automatically
    saveGameName = default

    // Scene to load. possible values:
    sceneToLoad = SpaceCenter // go to the Space Center
    // sceneToLoad = TrackingStation // go to the Tracking Station
    // sceneToLoad = FirstVessel // load the first found vessel into the flightscene (or Space Center if none is found)
}
